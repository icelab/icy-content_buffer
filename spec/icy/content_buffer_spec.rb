require 'spec_helper'

describe Icy::ContentBuffer do
  it 'has a version number' do
    expect(Icy::ContentBuffer::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
